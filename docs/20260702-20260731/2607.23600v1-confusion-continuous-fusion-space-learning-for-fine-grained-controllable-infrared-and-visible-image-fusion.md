---
title: "ConFusion: Continuous Fusion Space Learning for Fine-Grained Controllable Infrared and Visible Image Fusion"
title_zh: ConFusion：面向细粒度可控红外与可见光图像融合的连续融合空间学习
authors: "Guo Yurong, He Yufei, Li Yonghao, Chang Dongliang, Zhang Ke, Ma Zhanyu"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23600v1"
tags: ["query:ml"]
score: 9.0
evidence: 基于高斯条件调制的连续融合空间，实现细粒度可控红外可见光融合
tldr: 红外与可见光图像融合在安防、遥感等领域应用广泛，但可控融合面临离散控制条件导致的调制空间稀疏问题，难以满足细粒度需求。ConFusion提出学习连续融合空间，通过高斯条件实例调制变量与空间感知调制，结合Grounded SAM实例掩码引导，实现实例级细粒度可控融合。采用双分支网络解耦模态不变特征和模态特定特征，并利用文本引导语义对齐增强融合效果。推理时，多模态大语言模型将用户意图解析为实例级调制变量，实现灵活控制。实验表明，ConFusion在融合质量和下游任务多项指标上均取得领先性能，为细粒度可控图像融合开辟了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23600v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 792, \"height\": 906, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23600v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1654, \"height\": 942, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23600v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 933, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23600v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1657, \"height\": 714, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23600v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1662, \"height\": 723, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23600v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 806, \"height\": 858, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23600v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 810, \"height\": 317, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23600v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1342, \"height\": 378, \"label\": \"Table\"}]"
motivation: 现有可控融合依赖离散控制条件，导致调控空间稀疏，难以实现实例级细粒度调制，无法灵活适应多样化需求。
method: 提出ConFusion，学习连续融合空间，采用双分支解耦模态特征，通过高斯条件实例调制与基于Grounded SAM的掩码引导实现细粒度调控，并利用文本驱动增强语义对齐，推理时由多模态大语言模型解析用户意图。
result: ConFusion在融合质量和下游任务多项指标上达到最优，有效支持实例级细粒度可控融合。
conclusion: ConFusion为细粒度可控红外与可见光图像融合提供了有效方案，连续融合空间学习和实例调制具有潜力。
---

## 摘要
可控红外-可见光图像融合旨在通过灵活的区域感知调制，整合互补的热辐射与结构信息，生成适应多样化用户需求和下游任务的融合图像。然而，现有方法通常依赖预定义的离散控制条件，导致稀疏的解空间无法支持细粒度的调制需求。为此，我们提出ConFusion，一个通过高斯条件空间感知调制学习连续融合空间的新框架，实现实例级细粒度可控的红外与可见光图像融合。ConFusion采用双分支架构，在联合重建和文本引导的语义对齐下，解耦模态不变与模态特定表示。高斯条件实例调制变量与基于Grounded SAM的实例掩码相结合，通过掩码引导的特定特征调制器指导实例级细粒度调制，而文本驱动的不变特征增强器则提升语义一致性并增强融合。在推理阶段，多模态大语言模型将用户意图解析为实例级调制变量，以引导图像融合。大量实验表明，ConFusion在融合质量和下游任务的多个指标上均达到最先进性能，同时支持细粒度可控的图像融合。我们的代码已开源：https://github.com/HeyufeiAnto/Confusion

## Abstract
Controllable infrared-visible image fusion aims to integrate complementary thermal and structural information with flexible region-aware modulation, producing fused images that adapt to diverse user requirements and downstream tasks. However, existing methods typically rely on predefined discrete control conditions, leading to a sparse space that fails to support fine-grained modulation demands. To address this, we propose ConFusion, a novel framework that learns the continuous fusion space via Gaussian-conditioned spatial-aware modulation, enabling instance-level fine-grained controllable infrared and visible image fusion. ConFusion employs a dual-branch architecture to disentangle modality-invariant and modality-specific representations under joint reconstruction and text-guided semantic alignment. Gaussian-conditioned instance modulation variables coupled with Grounded SAM-based instance masks guide instance-level fine-grained modulation through the Mask-Guided Specific Feature Modulator, while the Text-Driven Invariant Feature Enhancer improves semantic consistency and enhances fusion. During inference, the multimodal large language model parses user intents into instance-level modulation variables to guide image fusion. Extensive experiments show that ConFusion achieves state-of-the-art performance across multiple metrics in both fusion quality and downstream tasks, while supporting fine-grained controllable image fusion. Our code is available at https://github.com/HeyufeiAnto/Confusion

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题背景**：红外与可见光图像融合（IVIF）在辅助驾驶、遥感、监控等场景应用广泛，旨在结合红外热辐射的鲁棒性与可见光纹理细节。
- **现有方法不足**：
  - 传统融合方法学习确定性的一对一映射，无法根据用户意图或任务需求灵活调节（非可控融合）。
  - 近期可控融合方法依赖预定义的离散文本/视觉提示，导致调控空间稀疏（粗粒度可控），难以支持“people with high thermal”与“people with extreme thermal”等细微语义差异的细粒度调制。
- **整体含义**：本文旨在构建一个**连续融合空间**，通过高斯条件的空间感知调制实现**实例级细粒度可控**红外与可见光图像融合，使同一实例区域能根据连续的用户意图平滑调整热辐射强度。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **总体框架**：ConFusion包含两个训练阶段和一个推理阶段，核心是学习连续融合空间，通过高斯采样的实例调制变量和文本引导语义对齐实现精细控制。
- **阶段一：解耦表征学习**（图2a）
  - **双分支特征解耦**：共享底层编码器后，由两个模态特定编码器学习热辐射、纹理等特定特征 `F_sp`，共享不变编码器学习场景语义不变特征 `F_in`。设计自重建与交叉重建约束保证解耦。
  - **解耦损失**：最小化互协方差度量以促进特定与不变特征的统计独立性。
  - **不变语义对齐**：利用Grounded SAM实例掩码对不变特征进行掩码平均池化，通过MLP投影后与CLIP文本嵌入 `E` 计算余弦相似度损失，约束不变特征与类别语义对齐。
- **阶段二：特征调制与增强融合**（图2b）
  - **掩码引导的特定特征调制器（MGSM）**：从截断高斯分布 `N[0,1](0.5, 0.22)` 为每个实例 `j` 采样调制强度 `α_j`，结合Grounded SAM的实例掩码构建空间调制掩码 `M`。对红外特定特征用 `M` 调制（缩放+偏移），对可见光特定特征用 `1-M` 调制，经Restormer块融合后得到调制后特定融合特征 `F_sp_mod`。
  - **文本驱动的不变特征增强器（TDIE）**：将模态不变特征 `F_in_ir` 和 `F_in_vi` 经Restormer块融合后，与CLIP文本嵌入通过交叉注意力交互，得到增强的不变融合特征 `F_in_enh`。
  - **损失函数**：采用ROI约束损失（在实例区域强制融合图像的热/可见像素强度倾向及边缘保留）和背景重建损失（约束非实例区域平衡保留双模态信息）。
- **推理阶段**：用户输入后，由多模态大语言模型解析为实例‑强度对和实例文本，通过Grounded SAM和CLIP获取 `M` 和 `E`，送入训练好的网络生成与意图对齐的融合图像。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **数据集**：
  - M³FD：4200对红外‑可见光图像（3900训练/300测试）
  - RoadScene：220对，仅测试
  - TNO：37对，仅测试
- **对比方法**（7种SOTA）：RFN‑Nest, TarDAL, CddFuse, CoCoNet, TDFusion (非可控融合)；TextFusion, RISFuse (粗粒度可控融合)。
- **评价指标**：PSNR、MSE、AG、SF、CC、QAB/F。
- **下游任务**：目标检测，在M³FD上重新划分3:1训练/测试，使用YOLOv9检测器，评估mAP@50、mAP@50:95、查全率、查准率等。
- **可控性分析实验**：通过不同用户意图文本，评测热辐射强度与意图的匹配程度（CC值），并比较TextFusion、RISFuse的多实例独立控制能力。

### 4. 资源与算力
- **硬件配置**：NVIDIA RTX 4090 GPU，AMD EPYC 7453 28‑core CPU。
- **软件环境**：PyTorch 2.9.1。
- **训练配置**：
  - 阶段一：50个epoch，批量大小2，输入裁剪224×224，Adam优化器，初始学习率1e‑4。
  - 阶段二：50个epoch，批量大小2，编码器/解码器学习率1e‑6，其余模块1e‑4。
- 未提及多卡并行训练，仅使用单卡RTX 4090。

### 5. 实验数量与充分性
- 实验包括：
  1. **定量对比**：在三个数据集上对7种方法进行6项指标测验（表1）。
  2. **定性对比**：可视化融合结果（图3）。
  3. **消融实验**：移除不变语义对齐、MGSM模块、TDIE模块、各损失函数（`L_cr`, `L_dis`, `L_inv`）共6组变体，在M³FD上比较6个指标（表2）。
  4. **可控性分析**：展示单实例与多实例在不同意图下的融合效果及CC值定量曲线（图4、图5）。
  5. **下游任务评估**：目标检测性能对比（表3）。
- **实验充分性**：覆盖多个基准数据集和不同类型的对比方法，消融实验详细验证了各模块和损失函数的贡献；可控性分析通过定量‑定性结合充分证明了细粒度控制的有效性。
- **公平性**：对比方法均按原文设定运行，可控制方法均采用图像特定文本输入，本方法采用统一意图由LLM解析，测试条件一致。

### 6. 论文的主要结论与发现
- ConFusion能够在连续融合空间中实现实例级的细粒度可控红外与可见光图像融合，用户意图可被精确映射到融合结果中。
- 所提出的双分支解耦与文本对齐、MGSM和TDIE模块显著提升了融合质量与语义一致性。
- 在M³FD、RoadScene、TNO三个数据集上，ConFusion在PSNR、MSE、AG、CC等多数指标上优于现有SOTA方法，并在目标检测下游任务中取得最优mAP。
- 相较于粗粒度可控方法，ConFusion对细微文本差异产生明显区分的热强度响应，且能独立精准控制多个实例，验证了连续融合空间的有效性。

### 7. 优点：方法或实验设计上的亮点
- **连续融合空间建模**：将实例级调制变量从离散集合拓展为高斯采样的连续空间，突破了粗粒度可控限制，支持任意细粒度用户意图。
- **精心设计的解耦与调制**：通过双分支解耦、统计独立性约束、文本语义对齐以及空间感知的调制策略，实现了可控性与融合质量的有效平衡。
- **端到端推理管线**：引入多模态大语言模型解析用户自由文本意图，并与Grounded SAM、CLIP等基础模型结合，无需预定义控制模板，使用灵活。
- **全面的实验验证**：涵盖融合质量、可控性、下游任务以及详尽的消融分析，对细粒度控制进行了定量指标（CC值）与可视化双重验证。

### 8. 不足与局限
- **计算负担**：推理阶段需调用大语言模型和Grounded SAM，引入额外推理开销，论文未提供推理延迟或模型参数量分析。
- **依赖外部模型精度**：实例调制变量生成依赖Grounded SAM的实例分割质量和大语言模型的意图解析，对分割错误或模糊意图的鲁棒性未深入探讨。
- **未探讨极端场景**：实验局限于已对齐的红外‑可见光数据集，未验证在恶劣天气、大视差或低信噪比条件下的稳定性。
- **潜在偏差**：截断高斯分布的超参数（均值0.5，方差0.22）为经验设定，可能影响连续空间的覆盖范围，缺乏灵敏度分析。

（完）
