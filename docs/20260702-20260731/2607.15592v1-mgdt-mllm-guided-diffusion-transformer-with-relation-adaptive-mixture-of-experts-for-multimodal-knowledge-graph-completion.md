---
title: "MGDT: MLLM-Guided Diffusion Transformer with Relation-Adaptive Mixture-of-Experts for Multimodal Knowledge Graph Completion"
title_zh: MGDT：面向多模态知识图谱补全的基于关系自适应专家混合的多模态大语言模型引导扩散Transformer
authors: "Xu Hou, Meiyu Liang, Wei Huang, Yawen Li, Zhe Xue, Wu Liu, Guanhua Ye, Lei Shi, Kangkang Lu"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15592v1"
tags: ["query:mre"]
score: 10.0
evidence: 提出多模态知识图谱补全框架，直接利用结构、文本和视觉线索提取关系。
tldr: 多模态知识图谱补全需融合结构、文本和视觉信息，但现有扩散方法直接对原始特征去噪，迫使模型同时完成线索选择、跨模态对齐和实体生成，导致大量噪声和语义不一致，制约性能。为此，MGDT提出创新性的先对齐后扩散框架：首先由关系自适应语义路由混合专家动态筛选关联模态并抑制干扰；接着利用冻结的多模态大语言模型将多源表示对齐到统一空间，消除跨模态异质性；最后由知识图谱扩散Transformer在此对齐空间进行图条件去噪，精准生成缺失实体。实验表明，MGDT在三个常用数据集上均显著超越现有基线，证明了该范式的有效性与优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15592v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 786, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1654, \"height\": 883, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 869, \"height\": 654, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 894, \"height\": 503, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15592v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1851, \"height\": 1134, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15592v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 809, \"height\": 458, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15592v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 758, \"height\": 242, \"label\": \"Table\"}]"
motivation: 现有基于扩散的MKGC方法直接在原始多模态特征上执行去噪，导致线索选择、对齐和生成任务混杂，引入噪声和语义不一致，影响补全效果。
method: 提出MGDT框架，首先使用关系自适应语义路由MoE选择相关模态，然后以冻结MLLM将多模态表示对齐到统一空间，最后用KGDT在图条件下扩散生成缺失实体。
result: 在三个基准数据集上，MGDT一致超越强基线方法，获得了最优的多模态知识图谱补全性能。
conclusion: 先对齐后扩散范式有效缓解了多模态噪声和异质性，使扩散模型能更准确生成缺失实体，提升了MKGC性能。
---

## 摘要
多模态知识图谱补全（MKGC）需要从结构、文本和视觉线索中推断缺失实体。现有的基于扩散的MKGC方法通常直接在原始多模态特征上进行去噪。这种设计迫使去噪器同时执行关系依赖的线索选择、跨模态语义对齐和结构感知的实体生成，这为扩散过程引入了噪声且语义不一致的条件，从而导致补全性能不佳。为了解决这一局限性，我们提出MGDT：基于关系自适应专家混合的多模态大语言模型引导扩散Transformer（MGDT），这是一个建立在先对齐后扩散范式上的新型MKGC框架。MGDT首先采用关系自适应语义路由专家混合（RASR-MoE）模块来选择与关系相关的多模态语义转换路径，并抑制不相关模态的干扰。然后，MGDT使用一个冻结的多模态大语言模型（MLLM）作为语义锚点，将路由后的多模态表示对齐到统一的潜在空间中，减少跨模态语义异质性。最后，知识图谱扩散Transformer（KGDT）在对齐后的空间中进行图条件去噪生成，以产生缺失的实体表示。在三个基准数据集上的实验表明，MGDT一致地优于强大的基线模型。

## Abstract
Multimodal Knowledge Graph Completion (MKGC) requires inferring missing entities from structural, textual, and visual cues. Existing diffusion-based MKGC methods usually denoise directly on raw multimodal features. Such a design forces the denoiser to simultaneously perform relation-dependent cue selection, cross-modal semantic alignment, and structure-aware entity generation, which introduces noisy and semantically inconsistent conditions for diffusion and consequently leads to suboptimal completion performance. To address this limitation, we propose MGDT: MLLM-Guided Diffusion Transformer with Relation-Adaptive Mixture-of-Experts (MGDT), a novel MKGC framework built on an align-then-diffuse paradigm. MGDT first employs a Relation-Adaptive Semantic Routing Mixture-of-Experts (RASR-MoE) module to select relation-relevant multimodal semantic transformation paths and suppress irrelevant modality interference. MGDT then uses a frozen Multimodal Large Language Model (MLLM) as a semantic anchor to align the routed multimodal representations into a unified latent space and reduce cross-modal semantic heterogeneity. Finally, a Knowledge Graph Diffusion Transformer (KGDT) performs graph-conditioned denoising generation in the aligned space to produce the missing entity representation. Experiments on three benchmark datasets show that MGDT consistently outperforms strong baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**: 论文聚焦于**多模态知识图谱补全（MKGC）**任务。现有基于扩散模型的方法通常直接在原始多模态特征（结构、文本、视觉）上进行去噪生成，这导致一个核心缺陷：迫使去噪模型**同时处理**关系依赖的线索选择、跨模态语义对齐和结构感知的实体生成三项复杂任务。
*   **问题后果**: 这种混杂的设计引入了**噪声大且语义不一致**的扩散条件，严重限制了模型的补全性能。
*   **整体含义（研究动机）**: 论文旨在解决上述次优问题，核心思路是解耦这些任务，提出一个“先对齐，后扩散”（align-then-diffuse）的新范式。其中心思想是：扩散模型不应该在原始异构的特征空间中进行，而应该在一个经过关系筛选和语义统一后的“干净”潜在空间中执行，从而让生成过程更专注、更高效。

### 2. 论文提出的方法论：核心思想、关键技术细节
论文的核心方法是 **MGDT（MLLM-Guided Diffusion Transformer）** 框架，包含三个关键模块：

*   **核心思想**:
    *   **解耦（Decoupling）**: 明确地将多模态线索选择、语义对齐与扩散生成过程分离开，使各个模块各司其职。
    *   **“先对齐-后扩散”范式**: 在将特征送入扩散模型进行生成之前，先对其进行关系自适应筛选和语义对齐，为扩散提供一个高质量的、统一的条件。

*   **关键技术细节**:
    1.  **关系自适应语义路由专家混合（RASR-MoE）**:
        *   **目的**: 解决“关系依赖的多模态线索选择”问题。不同关系（如“出生地”与“主演”）依赖不同模态的线索。
        *   **机制**: 对每一种模态（结构、文本、视觉），设置一个包含多个“专家”的池。一个由关系嵌入和模态特征共同控制的路由器，动态地为当前查询选择或组合最相关的专家。每个专家对输入特征进行关系感知的变换。最终，路由后的模态特征被融合，生成一个“干净”的多模态实体表示，为后续对齐做准备。
    2.  **多模态大语言模型（MLLM）锚定表示对齐**:
        *   **目的**: 解决“跨模态语义异质性”问题。引导RASR-MoE输出的特征进入一个语义统一的空间。
        *   **机制**: 使用一个**冻结的**预训练多模态大语言模型（如Qwen3-VL）作为“语义锚点”。原始多模态特征通过一个简单的投影层输入到冻结的MLLM，得到锚点特征。然后，通过余弦相似度损失或KL散度损失，拉近RASR-MoE模块输出的模态特征与MLLM锚点特征之间的距离，从而校准模型的内部表示，减少跨模态鸿沟。
    3.  **知识图谱扩散Transformer（KGDT）**:
        *   **目的**: 在对齐后的统一语义空间中，进行图条件引导的扩散生成，以产生缺失实体表示。
        *   **机制**: 采用标准的DDPM（去噪扩散概率模型）流程。去噪器是一个**Transformer**，它接收当前带噪的实体表示和由对齐后特征构成的条件。Transformer通过交叉注意力等机制，在对齐后的条件引导下，逐步从纯噪声中恢复出目标实体的精确嵌入。

### 3. 实验设计：数据集、Benchmark与对比方法
*   **数据集/场景**:
    *   使用了三个广泛使用的多模态知识图谱补全基准数据集：**MKG-W, MKG-Y, 和 DB15K**。
*   **对比方法 (Baselines)**:
    *   **单模态KGC方法(6个)**: TransE, DistMult, ComplEx, RotatE, GC-OTE, TuckER。
    *   **多模态KGC方法(13个)**: IKRL, TBKGC, TransAE, MMKRL, RSME, VBKGC, OTKGE, IMF, AdaMF-MAT, VISTA, MyGo, MoMoK, MCKGC。
    *   **评估指标**: 使用标准的信息检索指标，包括 **MRR (平均倒数排名), Hits@1, Hits@3, Hits@10**。

### 4. 资源与算力
论文明确说明了其资源与算力配置：
*   **框架**: PyTorch 2.6.0。
*   **硬件**: 在单块 **NVIDIA V100 GPU (32GB显存)** 上进行训练。
*   **训练详情**: 使用AdamW优化器，初始学习率为 $1×10^{-4}$，权重衰减为 $5×10^{-5}$，批大小为128，最大训练轮数为3000。输入特征中，文本由BERT-base初始化，视觉由预训练在ImageNet上的ResNet-50提取，结构嵌入由TuckER初始化。

### 5. 实验数量与充分性
实验设计较为全面和充分，能够支撑论文结论。
*   **主要性能对比**: 在3个数据集上与19个基线模型进行对比，涵盖了主要的单模态和多模态方法，对比范围广泛且公平。
*   **消融实验 (Ablation Study)**: 在MKG-W数据集上进行了详尽的消融研究，共设计了7种变体，分别移除了各模态分支、语义锚点、RASR-MoE模块以及替换/移除KGDT，清晰地论证了每个组件的独特贡献。
*   **超参数敏感性分析**: 在MKG-W数据集上分析了4个关键超参数（扩散步数、Transformer层数、MLLM对齐权重、扩散损失权重）对模型性能的影响，证实了模型在合理参数范围内的稳定性。
*   **泛化性分析**: 额外进行了量化分析和MLLM泛化性实验，通过替换不同类型的MLLM（CLIP, BLIP-2），验证了“先对齐-后扩散”范式的鲁棒性和可迁移性。

### 6. 论文的主要结论与发现
*   **性能优越**: MGDT在所有三个基准数据集的**MRR指标**上均达到最优，并在Hits@1和Hits@3上几乎全面领先，证明了“先对齐后扩散”范式的有效性。
*   **组件协同关键**: 消融实验表明，所有组件（各模态分支、RASR-MoE、语义锚点、KGDT）都对最终性能有正向贡献，移除任何一个都会导致性能下降，验证了各模块协同工作的必要性。其中，RASR-MoE的移除导致了最大幅度的性能下降，突显了关系感知模态路由的重要性。
*   **范式鲁棒**: 泛化性分析表明，即使更换不同的MLLM作为语义锚点，模型性能虽有波动，但依然保持竞争力，说明核心贡献在于提出的解耦设计范式，而非依赖某一特定MLLM。

### 7. 优点：方法或实验设计上的亮点
*   **问题建模清晰**: 准确识别并解耦了扩散模型在MKGC任务中的核心挑战（线索选择、对齐、生成混杂），为后续研究提供了新的思路。
*   **方法创新性强**: 将MoE（专家混合）、MLLM（多模态大语言模型）和扩散Transformer三者巧妙地结合在一个“先对齐后扩散”的框架中，模块各司其职，设计优雅。
*   **论证严谨**: 实验部分不仅包含全面的性能对比，还通过详尽的消融实验、超参数分析和泛化性实验，层层递进地验证了方法的有效性、稳定性和鲁棒性，说服力强。
*   **应用MLLM的方式巧妙**: 采用冻结的MLLM作为对齐的“语义锚点”，既利用了其强大的跨模态对齐先验知识，又避免了微调大模型带来的高昂计算成本。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
*   **计算效率**: 尽管MLLM被冻结，但其参与前向传播依然会带来可观的计算和显存开销，推理速度可能慢于传统的基于嵌入排序的方法。论文未详细讨论推理延迟。
*   **MLLM依赖性**: 模型的性能与所使用的MLLM能力直接挂钩，在缺乏合适开源MLLM的特定领域或低资源语言中，其应用效果可能受限。
*   **超参数敏感性**: 模型对对齐权重和扩散损失权重等超参数较为敏感，图5(c)(d)显示存在明显的最佳区间，实际应用中可能需要仔细调参。
*   **可解释性有限**: 尽管模型性能更好，但关系如何具体路由到不同专家，以及MLLM如何进行语义对齐，其内部决策过程仍是一个“黑箱”。
*   **数据集规模与领域**: 论文评估限于三个标准学术数据集，其在更大规模、更复杂工业场景下的效果和可扩展性有待进一步验证。

（完）
