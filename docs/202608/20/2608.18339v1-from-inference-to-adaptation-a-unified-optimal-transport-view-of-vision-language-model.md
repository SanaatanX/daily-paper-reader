---
title: "From Inference to Adaptation: A Unified Optimal Transport View of Vision Language Model"
title_zh: 从推理到自适应：视觉语言模型的统一最优传输视角
authors: "Qi Yu, Zhichen Zeng, Katherine Tieu, Xiyuan Yang, Ruizhong Qiu, Yuchen Yan, Lihui Liu, Yanjun Zhao, Lingjie Chen, Jingrui He, Hanghang Tong"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.18339v1"
tags: ["query:ml"]
score: 8.0
evidence: 利用最优传输建模模态间样本级关系，用于视觉-语言模型测试时自适应
tldr: "视觉语言模型在测试时易受分布偏移影响，现有测试时自适应方法依赖原始嵌入相似度的噪声伪标签，且代理目标与推理目标不一致。本文提出一种基于Wasserstein最优传输的TTA方法，将零样本推理建模为跨模态对齐以生成样本级可靠伪标签，并用软标签InfoNCE损失进行自适应。理论表明InfoNCE可重写为最优传输形式，统一推理与自适应目标，性能提升达7%且高效。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.18339v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1425, \"height\": 592, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18339v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1425, \"height\": 600, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18339v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 340, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18339v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 714, \"height\": 358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18339v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1440, \"height\": 466, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.18339v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 1423, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.18339v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 697, \"height\": 417, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.18339v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 484, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.18339v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 1337, \"label\": \"Table\"}]"
motivation: 现有VLM测试时自适应方法依赖不可靠伪标签和粗粒度代理目标，导致推理与自适应目标不一致，性能提升有限。
method: 用Wasserstein最优传输将零样本推理建模为跨模态对齐生成伪标签，并基于此用软标签InfoNCE损失自适应，理论证明两者目标可统一。
result: "在多个基准上超越最佳方法达7%，同时保持最优效率。"
conclusion: 统一推理与自适应的最优传输视角为VLM测试时自适应提供了有效且高效的方案。
---

## 摘要
视觉语言模型（VLMs）已经展现出卓越的零样本能力，但在推理过程中对真实世界的分布变化仍然敏感。尽管在测试时对VLMs进行自适应方面付出了很多努力，但它们严重依赖于推理过程中直接从原始嵌入相似性预测的嘈杂伪标签，这些标签在分布变化下不可靠，并误导自适应。为了避免噪声放大，现有工作在自适应期间设计粗粒度的替代目标，未能显式建模跨不同模态的样本级关系，导致与推理的目标不匹配，从而产生边际性能改进。在这项工作中，我们旨在弥合VLMs推理和自适应之间的分离目标，并提出一种称为\algname的原理性VLM测试时自适应（TTA）方法。对于VLM推理，我们将零样本图像分类任务制定为通过Wasserstein OT公式编码的跨模态对齐问题，在样本级提供稳健的伪标签，以有效地自适应VLMs。对于VLM自适应，我们采用软标签InfoNCE损失，基于OT诱导的伪标签来适应VLMs，利用细粒度监督通过对比学习显式建模个体图像-文本对的关系，从而在同粒度上实现准确推理。此外，我们从理论上揭示了InfoNCE损失可以巧妙地重新表述为Wasserstein OT公式，从而统一VLMs推理和自适应的目标，实现它们的互惠互利。大量实验证明了我们方法的有效性和效率，以最先进的效率，性能优于最佳方法高达7%。

## Abstract
Vision-language models (VLMs) have demonstrated remarkable zero-shot capabilities yet remain sensitive to real-world distribution shifts during inference. Although significant efforts are devoted to adapting VLMs at test time, they rely heavily on noisy pseudo-labels predicted directly from raw embedding similarities during inference, which are unreliable under distribution shift and mislead the adaptation. To avoid noise amplification, existing works craft coarse-grained surrogate objectives during adaptation, which fail to explicitly model sample-level relationships across different modalities, creating objective mismatch with inference, thus leading to marginal performance improvement. In this work, we aim to bridge the detached objectives of inference and adaptation for VLMs, and propose a principled VLM TTA method called \algname. For VLM inference, we formulate the zero-shot image classification task as a cross-modal alignment problem encoded via a Wasserstein OT formulation, providing robust pseudo-labels at the sample-level to effectively adapt VLMs. For VLM adaptation, we adopt a soft-label InfoNCE loss to adapt VLMs based on the OT-induced pseudo-labels, leveraging fine-grained supervisions to explicitly model relationships of individual image-text pairs via contrastive learning, which empowers accurate inference at the same granularity. Moreover, we theoretically reveal that the InfoNCE loss can be neatly reformulated as a Wasserstein OT formulation, thereby unifying the objectives of the inference and adaptation of VLMs to achieve their mutual benefits. Extensive experiments demonstrate the effectiveness and efficiency of our methods, outperforming the best-performing methods by up to 7% with state-of-the-art efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《From Inference to Adaptation: A Unified Optimal Transport View of Vision Language Model》的详细中文总结。

## 1. 论文的核心问题与整体含义

- **研究背景**：视觉语言模型（VLM，如 CLIP）通过将图像和文本映射到共享嵌入空间，在零样本分类等任务上表现出色。然而，真实世界中的分布偏移（如图像损坏）会导致嵌入空间失效，造成性能显著下降。
- **核心问题**：测试时自适应（TTA）是应对分布偏移的主流范式，但现有方法存在两大局限：
  1. **伪标签不可靠**：现有方法依赖从原始嵌入相似度直接推理的零样本预测作为伪标签，在分布偏移下噪声极大，容易误导后续自适应过程。
  2. **目标不一致**：为了避免噪声放大，许多方法使用基于类级原型的粗粒度替代目标（如平均类嵌入），无法显式建模单个图像-文本对的样本级跨模态关系，导致自适应目标与推理任务（样本级对齐）不匹配，性能提升有限。
- **整体含义**：论文提出，推理和自适应不应是割裂的两个阶段，而应通过最优传输（Optimal Transport, OT）统一为一个互惠互利的整体，从而生成更可靠的伪标签、更有效的自适应目标，最终提升 VLM 在分布偏移下的测试时自适应性能。

## 2. 论文提出的方法论

- **方法名称**：ORIGIN。
- **核心思想**：利用 Wasserstein 最优传输统一 VLM 的推理与自适应目标。
- **推理阶段（OT-based Inference）**：
  - 将零样本图像分类建模为跨模态（图像-文本）对齐问题。
  - 给定一批测试图像和文本类描述，构造传输代价矩阵 `C_{i,j} = 1 - v_i^T t_j`（即余弦相似度的补）。
  - 求解带熵正则的 Wasserstein OT 问题：`min <C,S> - εH(S)`，其中 S 为传输计划。
  - 用 Sinkhorn 算法高效求解，得到传输计划 S 后，每张图像的预测标签由 `argmax_j S_{i,j}` 得到。
  - 文本侧边际分布通过指数移动平均（EMA）动态调整，以适应测试流中类别分布的变化。
  - OT 的全局约束特性可抑制嵌入相似度噪声，生成更准确的伪标签。
- **自适应阶段（Fine-grained Adaptation）**：
  - 采用软标签 InfoNCE 损失：`min_θ KL(S || P̃_θ)`，其中 `P̃_θ(i,j) = exp(v_i^T t_j / τ) / Σ_k exp(v_i^T t_k / τ)`。
  - S 为推理阶段 OT 求得的传输计划，作为软伪标签。
  - 该损失显式建模样本级图像-文本关系，与 CLIP 预训练目标（对比学习）保持一致。
  - 仅调整图像编码器中的归一化层（如 LayerNorm），冻结其他参数，以适应图像侧分布偏移。
- **统一推理与自适应**（Lemma 3.1）：
  - 论文理论上证明软标签 InfoNCE 损失可以重写为熵正则 Wasserstein OT 项加上一个与传输计划 S 无关的常数项。
  - 因此，优化 S（推理）和优化 θ（自适应）共享同一个目标函数；推理产生的伪标签帮助自适应，自适应的参数更新也反过来提升推理准确性。
  - 通过交替优化 S 和 θ，实现推理与自适应的互惠互利。

## 3. 实验设计

- **数据集与基准**：
  - 使用三个标准 VLM TTA 基准：**CIFAR10-C**、**CIFAR100-C**、**ImageNet-C**。
  - 每个数据集包含 15 种常见损坏（噪声、模糊、天气、数字类等），实验采用最高严重级别（Level 5）。
  - 评估指标为分类准确率。
- **VLM 主干**：
  - 主要使用 CLIP ViT-B-16，另补充 ViT-B-32 的实验结果。
- **对比方法**：
  - **训练无关方法**：TDA、DMN、VTE、ZERO、ECALP、TENT。
  - **基于训练的方法**：RoTTA、TPT、MEMO、WATT、MINT、BATCLIP（其中 MINT 和 BATCLIP 为最强基线）。
- **后续分析实验**：
  - 消融变体：EMB/OT（无自适应的推理方式）、EMB+M、EMB+B、EMB+I、OT+M、OT+B、ORIGIN，用于分离推理和自适应各自贡献。
  - 超参数敏感性分析：温度 τ（即熵正则权重 ε）、EMA 参数 α、批次大小 b。
  - t-SNE 嵌入可视化，对比不同方法的图像/文本嵌入对齐情况。

## 4. 资源与算力

- 论文在附录 B 中说明了实验机器配置：
  - 双路 Intel Xeon Gold 6240R CPU，4 张 NVIDIA Tesla V100-SXM2 32GB GPU。
- 未报告具体的训练时长、总 GPU 小时数等详细计量。
- 未提供明确的“每轮实验耗时”或“总计算量”数据。

## 5. 实验数量与充分性

- **实验组数**：
  - 两个主干（ViT-B-16、ViT-B-32）× 三个数据集的主 benchmark 结果（每数据集 15 类损坏）。
  - 效率-精度 trade-off 实验（CIFAR10-C、CIFAR100-C）。
  - 消融实验（8 种变体，在 CIFAR10-C 上按 4 类损坏汇总）。
  - 超参数敏感性（温度、EMA、批次大小，各 3-4 组设置）。
  - t-SNE 可视化。
- **充分性评估**：
  - 实验覆盖了主要基准、多种方法类别、消融和敏感性分析，整体较充分。
  - 结果报告为 5 次不同随机数据加载器运行的平均值，具备一定的统计可靠性。
  - 对比方法的超参数使用官方默认设置，批次大小对齐，整体公平性较好。
  - 但消融实验仅在 CIFAR10-C 上进行，缺少在其他数据集上的消融验证；对于 ViT-B-32 只报告主结果，未做消融。

## 6. 论文的主要结论与发现

- ORIGIN 在三个基准上均取得最佳平均准确率，相比最强基线提升最高达 7.4%（CIFAR10-C）、4.8%（CIFAR100-C）、2.4%（ImageNet-C）。
- OT 推理本身（不进行自适应）即可优于许多训练无关的启发式方法，生成的伪标签更可靠。
- 软标签 InfoNCE 损失相比类级替代目标（MINT、BATCLIP）能提供更高的自适应性能上限，平均提升约 9.64%（vs MINT）和 6.96%（vs BATCLIP）。
- 推理与自适应在统一 OT 框架下互惠互利，交替优化带来一致的性能提升。
- ORIGIN 的计算开销可忽略不计，在效率-精度散点图中位于左上角，同时达到 SOTA 效果和最快的运行时间（相比部分方法可达 45 倍加速）。
- ORIGIN 对超参数选择不敏感，在温度、EMA、批次大小变化下性能稳定。

## 7. 优点

- **理论创新**：首次从最优传输视角将 VLM 的推理与自适应目标统一为同一优化问题，提供了严谨的数学证明（Lemma 3.1）。
- **方法设计合理**：OT 提供全局约束、去噪的伪标签；InfoNCE 与 CLIP 预训练目标一致，利用样本级监督信号，而非粗糙的类级原型。
- **实用高效**：Sinkhorn 求解快速，且只微调归一化层，额外开销极小，同时显著提升性能。
- **实验全面**：多数据集、多主干、多基准确认有效性；包含效率分析和消融/敏感性研究，验证了各模块的贡献。

## 8. 不足与局限

- **适用范围受限**：ORIGIN 是**在线 TTA**方法，不直接适用于严格的情节式（episodic）TTA 设置，需要显式维护历史样本记忆。
- **需要梯度访问**：方法依赖模型梯度进行测试时参数更新，对于封闭源码的商用模型不适用。
- **消融范围有限**：核心消融实验仅基于 CIFAR10-C，缺少在 CIFAR100-C 和 ImageNet-C 上的分解验证，可能削弱结论的普适性。
- **文本侧偏移处理不足**：默认仅对齐图像侧嵌入，虽然理论上可扩展到文本侧偏移，但实验未覆盖文本分布偏移场景。
- **统计细节**：虽报告了 5 次运行平均结果，但未在图表中给出误差条或置信区间，统计显著性证据较弱。
- **未释放代码**：论文声称代码将随后发布，当前无法直接复现实验。

（完）
