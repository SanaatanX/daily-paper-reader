---
title: Multimodal Alignment Through Joint Kernel Entropic Gromov--Wasserstein Optimal Transport
title_zh: 联合核熵Gromov--Wasserstein最优输运的多模态对齐
authors: "Yixuan Florence Wu, Yilun Zhu, Naichen Shi"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.04234v1"
tags: ["query:ml"]
score: 9.0
evidence: 利用最优传输将多种模态对齐到共享潜在空间，实现结构保持的跨模态对齐
tldr: "多模态对齐常因跨模态配对数据稀缺而困难，现有方法多依赖原始特征距离。提出联合核熵Gromov-Wasserstein最优传输（JK-EGW），利用模态内和跨模态的细粒度相似性构造全局亲和核，在保持几何结构的同时对齐嵌入空间。理论上达到n^{-1/2}的样本复杂度，算法结合低秩核近似与变分提升实现高效求解。在少样本多模态检索任务中，性能优于现有对齐方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04234v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1114, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04234v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1584, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04234v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1413, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04234v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1445, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04234v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1549, \"height\": 798, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04234v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 519, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04234v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 905, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04234v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1258, \"height\": 908, \"label\": \"Table\"}]"
motivation: 跨模态配对数据稀缺，现有对齐依赖原始特征距离，难以利用预训练编码器中的结构信息，导致对齐效果受限。
method: 提出JK-EGW，基于二次最优传输目标，用细粒度相似性构建全局亲和核，并通过低秩近似和变分提升交替优化求解。
result: "在多模态检索少数据场景下，方法优于现有基线，并获得n^{-1/2}的样本复杂度理论保证。"
conclusion: JK-EGW为数据稀缺下的多模态对齐提供了结构保持且高效可扩展的解决方案。
---

## 摘要
我们研究了将来自多种模态的数据对齐到共享表示空间的问题，重点关注强预训练单模态编码器可用但跨模态配对数据稀缺的场景。我们提出了一种保持结构对齐的框架，即联合核熵Gromov--Wasserstein最优输运（JK-EGW），通过最小化二次最优输运目标将多种模态映射到共同潜在空间。JK-EGW利用模态内和跨模态的细粒度相似性关系来构建全局亲和核，而非依赖原始特征空间距离。我们的框架自然地提供了对潜在嵌入几何和分布的显式控制。在理论方面，我们建立了参数样本复杂度为$n^{-1/2}$的速率，与标准、熵和Gromov--Wasserstein最优输运的相应速率一致。在算法方面，我们推导出一种可扩展的交替过程，通过低秩核近似和变分提升，使用熵最优输运（EOT）更新来求解JK-EGW。这种提升方案有效减轻了二次目标的负担，并使我们能够利用现有EOT求解器的优势。在实验上，我们专注于数据稀缺情况下预训练编码器嵌入的后验对齐，并表明我们提出的方法相比现有对齐基线实现了更好的多模态检索性能。

## Abstract
We study the problem of aligning data from multiple modalities into a shared representation space, focusing on settings where strong pretrained unimodal encoders are available but cross-modal paired data are scarce. We propose a structure-preserving alignment framework, joint kernel entropic Gromov--Wasserstein Optimal Transport (JK-EGW), which maps multiple modalities into a common latent space by minimizing a quadratic optimal transport objective. JK-EGW leverages fine-grained similarity relationships within and across modalities to construct a global affinity kernel instead of relying on raw feature-space distances. Our framework naturally provides explicit control over the geometry and distribution of the latent embedding. On the theory side, we establish parametric sample complexity rate of $n^{-1/2}$, matching the corresponding rates for standard, entropic and Gromov--Wasserstein optimal transport. On the algorithmic side, we derive a scalable alternating procedure to solve JK-EGW with entropic optimal transport (EOT) updates through a low-rank kernel approximation and a variational lifting. This lifting scheme effectively relieves the burden of a quadratic objective, and allowing us to take the advantage of existing EOT solvers. Empirically, we focus on post-hoc alignment of embeddings from pretrained encoders in data-scarce regimes, and show that our proposed method achieves improved multimodal retrieval performance compared to existing alignment baselines.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义

- **研究背景**：多模态学习旨在整合来自图像、文本、音频等多种异构数据源的信息，而核心挑战在于如何将不同模态的嵌入表示对齐到共享空间中，使语义相关的观测变得可比较。
- **现实痛点**：大规模联合预训练成本高昂；虽已有大量利用冻结预训练单模态编码器进行轻量级后验对齐的尝试，但现有方法普遍存在两个问题：
  - 依赖原始特征空间距离，难以反映不同模态间的细粒度语义关系；
  - 对共享潜在空间的几何与边际分布缺乏显式控制，可能出现各向异性、畸变甚至模式坍缩。
- **研究问题**：能否在保持多模态语义结构的同时，使联合潜在表示服从预设的边缘分布？
- **核心思路**：从Gromov--Wasserstein最优输运（GWOT）视角出发，通过构造全局亲和核（global affinity kernel）统一描述模态内与跨模态的相似性，并以此驱动多模态对齐。

### 2. 方法论

- **核心框架**：提出联合核熵Gromov--Wasserstein最优输运（JK-EGW）目标函数：
  - 在全局亲和核 \(K(x,x')\) 的加权下，最小化耦合样本在潜在空间中的距离平方；
  - 加入熵正则项（KL散度）保证优化可行性；
  - 每种模态通过独立耦合 \(\pi_i\) 与共享的潜在参考分布 \(\nu\) 对齐。
- **全局亲和核**：
  - 定义在全体模态空间的非交并上；
  - 模态内相似性由特征相似度刻画，跨模态相似性可由类别标签、专家知识或预训练表示构造。
- **有限秩核近似**：
  - 假设 \(K\) 为对称半正定核，满足 Mercer 分解条件；
  - 用前 \(R\) 个特征函数构造秩\(R\)近似 \(K_R(x,x') = \Phi_R(x)^\top \Phi_R(x')\)；
  - 证明了截断误差由 Mercer 尾部量 \(\eta_R\) 控制（Proposition 3）。
- **变分提升（Variational Lifting）**：
  - 将秩\(R\)二次GW目标等价转化为一个线性EOT问题加上辅助矩阵\(A\)的最小化（Theorem 4）；
  - 辅助变量\(A\)捕获聚合特征-潜在矩，内层是标准熵最优输运（EOT）子问题。
- **交替优化算法（Algorithm 1）**：
  1. 固定辅助矩阵 \(\hat{A}\)，计算线性成本，使用 Sinkhorn 算法并行求解各模态的 EOT 子问题；
  2. 固定传输计划，\(\hat{A}\) 有闭式解 \(\hat{A} = L^\top \hat{\Pi} \hat{Z}\)；
  3. 迭代更新直至收敛。
- **低秩近似**：通过 Pivoted Cholesky / Nyström 分解高效构造经验核矩阵的低秩因子 \(L\)，计算复杂度为 \(O(N \hat{R}^2)\)，存储为 \(O(N \hat{R})\)。

### 3. 实验设计

- **实验一：多模态手写数字（Multi-modality Numerals）**
  - 数据集：UCI Multiple Features（76维傅里叶系数 + 64维Karhunen--Loève系数，作为两种模态）；
  - 用途：概念验证，可视化传输计划学习到的潜在嵌入是否按数字类别聚集。
- **实验二：合成数据样本复杂度验证**
  - 构造两组离散高斯混合分布作为模态分布，潜在参考为均匀网格分布；
  - 验证理论样本复杂度 \(O(n^{-1/2})\) 的收敛速率；
  - 验证不同核（RBF、指数核、有理二次核、周期核）下截断误差随秩 \(R\) 的衰减趋势。
- **实验三：MS-COCO 图像-文本对齐**
  - 数据：MS-COCO 2017训练集构建两个子集（6K：20类、每类300训练图；9K：30类、每类300训练图）；
  - 编码器：冻结的 MPNet（文本）和 DINOv3（图像）；
  - 潜在空间：20分量高斯混合模型，维度 \(d=32\)；
  - 评估指标：
    - 对齐质量：聚类纯度（purity）、轮廓系数（silhouette）；
    - 跨模态检索：R@1、R@5（图像→文本与文本→图像的平均值）；
  - 对比基线：CSA、CSA+STRUCTURE、MLP+STRUCTURE；
  - 另附潜在维度 \(d \in \{32, 48, 64, 128\}\) 的消融实验（附录E）。

### 4. 资源与算力

- **论文未明确说明**训练所使用的 GPU 型号、数量、训练时长或计算资源规模。
- 仅可推断：由于所有特征均为预提取冻结特征，对齐阶段的计算量较轻（每轮 Sinkhorn 更新为 \(O(Mn^2)\)），整体算力需求应远低于端到端多模态预训练。

### 5. 实验数量与充分性

- **实验数量**：
  - 3类主要实验（手写数字可视化、合成数据验证、MS-COCO对齐）；
  - 两组MS-COCO规模（6K与9K）；
  - 4种核函数的截断误差对比；
  - 4个潜在维度（32/48/64/128）的消融实验；
  - 100次蒙特卡洛重复的样本复杂度实验。
- **充分性评估**：
  - 优点：合成实验直接验证理论速率，消融实验覆盖关键超参数，基线选择为近年代表性方法，评估指标兼顾聚类质量与检索性能；
  - 不足：真实数据实验仅涉及图像-文本两模态，未覆盖音频、传感器等更多模态；未报告多次运行的标准差或统计显著性检验；纯度指标在9K子集上JK-EGW并不总是最优，说明方法在不同数据规模下各有侧重。

### 6. 主要结论

- **理论层面**：全核JK-EGW的经验目标与总体目标之间的偏差以 \(O(n^{-1/2})\) 参数速率收敛（Theorem 5），与标准/熵/GW最优输运的最优速率一致；误差包含统计项与核截断项，统计复杂度由 \(R \vee d_z\) 决定而非原始特征维度。
- **算法层面**：低秩核近似与变分提升将非凸二次问题转化为交替求解EOT子问题的形式，每个子问题均可由现成Sinkhorn求解器高效处理。
- **实验层面**：
  - 在MS-COCO 6K与9K上，JK-EGW在R@1和R@5上全面优于所有基线；
  - JK-EGW是唯一在所有设置下获得正轮廓系数的方法，显示出更优的几何一致性；
  - 潜在维度消融显示其性能优势在 \(d \in [32,128]\) 范围内总体稳定。

### 7. 优点

- **理论贡献扎实**：首次为核化多模态对齐建立了 \(n^{-1/2}\) 参数样本复杂度，并区分了统计误差与核截断误差，分析不依赖原始特征空间的维度或尾条件。
- **方法论有新意**：全局亲和核统一模态内/跨模态结构；有限秩Mercer分解与变分提升将非凸二次问题转化为成熟的EOT问题，兼具理论可分析性与算法可扩展性。
- **几何可控性**：可显式指定潜在空间边际分布（如GMM），实现对嵌入几何的主动塑造。
- **数据效率高**：专为少样本跨模态配对场景设计，符合后验对齐冻结编码器的实际需求。
- **实验验证较完整**：从概念验证、理论验证到真实数据集评测，构成较完整证据链。

### 8. 不足与局限

- **算力信息缺失**：未报告任何硬件配置与运行时间，无法判断实际部署成本。
- **真实实验规模有限**：仅MS-COCO图像-文本场景，未覆盖更多模态对或更大规模数据；6K/9K子集属于中等规模。
- **评估集仅依赖单数据集**：缺少如Flickr30K、CC3M等其他公开基准的交叉验证。
- **纯度指标不一致**：在9K子集上JK-EGW的纯度低于CSA类基线，说明方法在类别紧凑性上并非全面占优。
- **传输映射外推依赖MLP**：测试阶段需训练MLP逼近训练集上的传输映射，可能引入额外的近似误差，论文对这一点讨论有限。
- **核函数与\(\nu\)选择需要先验**：全局亲和核的构造依赖标签或人工定义相似度；参考分布\(\nu\)的选择策略未系统探讨。
- **未验证超大模态数**：理论虽考虑一般\(M\)，但实验未验证 \(M>2\) 场景。

---

（完）
